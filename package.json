{
  "name": "check-in-luv",
  "private": true,
  "version": "0.1.0",
  "description": "Private couple check-in PWA with Fastify API and admin dashboard.",
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/admin"
  ],
  "scripts": {
    "dev": "npm run dev -w @lovecheck/api",
    "dev:api": "npm run dev -w @lovecheck/api",
    "dev:web": "npm run dev -w @lovecheck/web",
    "dev:admin": "npm run dev -w @lovecheck/admin",
    "build": "npm run build -w @lovecheck/api && npm run build -w @lovecheck/web && npm run build -w @lovecheck/admin",
    "build:apk": "npm run android:apk",
    "typecheck": "npm run typecheck -w @lovecheck/api && npm run typecheck -w @lovecheck/web && npm run typecheck -w @lovecheck/admin",
    "icons": "node scripts/generate-icons.mjs",
    "android:assets": "node scripts/generate-android-assets.mjs",
    "android:sync": "npm run build -w @lovecheck/web && npx cap sync android && npm run android:assets",
    "android:apk": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.34.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {}
}
